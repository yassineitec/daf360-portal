# ============================================================
# Local development credentials — DO NOT COMMIT THIS FILE
# Add "application-local.yml" to your .gitignore
# Run with: SPRING_PROFILES_ACTIVE=local mvn spring-boot:run
# ============================================================

spring:
  datasource:
    username: sa
    password: "Timesheetdev2026!**"

  jpa:
    hibernate:
      ddl-auto: update

  security:
    oauth2:
      client:
        registration:
          azure:
            client-id:     0e4280e9-aba1-4ab7-a67f-60319e54c3bb
            client-secret: "l.X8Q~XJoPKpCF1xfFr-Dmn00D0jPf6JuF8wIcV~"
        provider:
          azure:
            issuer-uri: https://login.microsoftonline.com/d4a96e86-7cb0-4ad5-8ad8-2f613af98ce8/v2.0

server:
  servlet:
    session:
      cookie:
        same-site: lax    # OAuth2 callback needs Lax — Strict blocks the redirect from Azure AD
        secure: false     # Must be false on plain HTTP (localhost)
        http-only: true

app:
  jwt-secret: "daf360-local-dev-jwt-secret-key-min-32-chars"
  frontend-url: http://localhost:4200
  jwt:
    private-key-path: C:/Users/MSI/Desktop/daf360-portal/keys/private_pkcs8.pem
    public-key-path: C:/Users/MSI/Desktop/daf360-portal/keys/public.pem
  rh-db:
    url: "jdbc:sqlserver://localhost:1433;databaseName=DAF360_HR;encrypt=false;trustServerCertificate=true"
    username: sa
    password: "Timesheetdev2026!**"
